# Service Entry 示例

语言：[English](README.md) | 中文

这个 bundle 是一个最小的引擎级 service entry 契约示例。它演示：

- project manifest 结构
- service entry 文件引用
- input 和 output schema 文件
- 声明式 interval trigger
- service 代码输出 artifact

校验方式：

```bash
agent-compose bundle validate ./examples/service-entry
```
